{% set name = "hcephes" %}
{% set version = "0.2.0" %}
{% set sha256 = "ee559160404fed3b5dc9cd15f0d69053635e5de6027efee76f1be768810acd5192a9af3bc213e2068ffb4dd6c02deec4c5b3652d56da8602969c761c4080f845" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/limix/hcephes/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc9   # [win and py27]
    - vc10  # [win and py34]
    - vc14  # [win and py35]
    - vc14  # [win and py36]

requirements:
  build:
    - python  # [win]
    - toolchain
    - cmake
    - vc 9   # [win and py27]
    - vc 10  # [win and py34]
    - vc 14  # [win and py35]
    - vc 14  # [win and py36]
  run:
    - vc 9   # [win and py27]
    - vc 10  # [win and py34]
    - vc 14  # [win and py35]
    - vc 14  # [win and py36]

test:
  commands:
    - test -e $PREFIX/lib/libhcephes.so  # [linux]
    - test -e $PREFIX/lib/libhcephes.dylib  # [osx]
    - test -e $PREFIX/lib/libhcephes_static.a  # [unix]
    - if NOT exist %PREFIX%/lib/hcephes.lib exit 1  # [win]
    - if NOT exist %PREFIX%/lib/hcephes.dll exit 1  # [win]
    - if NOT exist %PREFIX%/lib/hcephes_static.lib exit 1  # [win]

about:
  home: https://github.com/limix/hcephes
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Cephes library'
  doc_url: https://github.com/limix/hcephes
  dev_url: https://github.com/limix/hcephes

extra:
  recipe-maintainers:
    - Horta
